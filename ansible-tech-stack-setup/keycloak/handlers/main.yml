# Handler to restart keycloak when notified
  - name: Reload systemd
    ansible.builtin.systemd:
      daemon_reload: yes

  - name: Restart keycloak
    ansible.builtin.systemd:
      name: keycloak
      state: restarted
      enabled: yes